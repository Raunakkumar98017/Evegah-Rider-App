import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors (from design spec)
  static const Color primaryPurple = Color(0xFF4B1DB8);   // Primary Purple
  static const Color limeGreen = Color(0xFFD8F238);        // Lime Green / CTA
  static const Color backgroundLight = Color(0xFFF8F8FA);  // Background

  // Legacy aliases for backward compatibility
  static const Color primary = Color(0xFF095D2D);
  static const Color primaryLight = Color(0xFF1B8246);
  static const Color primaryDark = Color(0xFF053B1C);
  static const Color accent = Color(0xFF10B981);

  // Wallet Card
  static const Color walletCardBg = Color(0xFF331879);

  // Deep Purple variants
  static const Color deepPurple = Color(0xFF3E1E90);
  static const Color purpleAccent = Color(0xFF5B30A6);

  // Background and Surfaces
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFF4F9F5);
  static const Color scaffoldBackground = Color(0xFFFFFFFF);

  // Typography & Neutrals
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF4B5563);
  static const Color textMuted = Color(0xFF9CA3AF);
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFF3F4F6);

  // Action / Feedback
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);
}
